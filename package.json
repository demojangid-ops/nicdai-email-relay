{
  "name": "nicdai-cloudflare-email-relay",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check src/index.js && node --test test/*.test.js",
    "dev": "wrangler dev",
    "prepare:deploy-config": "node scripts/prepare-deploy-config.mjs",
    "deploy": "npm run prepare:deploy-config && wrangler deploy --config .wrangler.deploy.jsonc",
    "build": "wrangler deploy --dry-run --outdir .wrangler-dist"
  },
  "devDependencies": {
    "wrangler": "4.119.0"
  }
}
